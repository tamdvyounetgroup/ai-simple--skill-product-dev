case 0 A=partial B=fail — A: đúng hướng (chọn profile core không bật cả 15, init, CLAUDE.md mỏng, hook 4 check, doctor gợi profile kế, không viết code tính năng) nhưng thiếu `--stack supabase`, app-map không nêu covers/last_verified/ttl_days, thiếu doc-health, không nêu trục Git-giữ-sự-thật vs ai-simple-quản-context. B: tư vấn generic (CLAUDE.md, husky, schema trước, spec/feature), không init lớp máy, không profile, không app-map covers/TTL, không hook covers-sync — sai hướng.
case 1 A=partial B=partial — A: đúng chẩn đoán NT01/02/03 (root <6K trỏ module, app-map, router /fl, không đổi model chỉ thêm lớp context) nhưng thiếu covers: trên app-map và cổng máy covers-sync pre-commit + doc-health TTL (chỉ nêu coupling map/doc-status). B: đúng hướng cấu trúc (app-map, CLAUDE.md con, ADR, dồn quyết định vào repo) nhưng thiếu covers:, router, và hoàn toàn thiếu cổng máy chống mục.
case 2 A=pass B=partial — A: đủ GREEN/YELLOW/RED đúng định nghĩa, RED hỏi đúng 1 câu gộp, trục authority độc lập (reversible ≠ authorized: gửi tin/publish/deploy/prod cần cấp quyền), kết luận giảm hỏi bằng phân tầng. B: có 3 tầng + gộp 1 câu + assumptions, nhưng không tách trục authority độc lập với risk (gửi mail chỉ bị xếp chung vào "không hoàn tác"), định nghĩa YELLOW hơi lệch (đổi API công khai/schema migration).
case 3 A=partial B=fail — A: đủ lot MECE, claim lease qua `ai-simple parallel`, worktree/branch riêng, merge queue, cấm stash/reset/checkout, không tự chế khoá; thiếu claim gate trong pre-commit chặn commit đụng path đã claim và luật revert chỉ qua patch-file. B: đúng hướng worktree + chia phạm vi + merge tuần tự + cấm reset/checkout, nhưng không nêu `ai-simple parallel`/registry claim/lease/claim gate và còn gợi ý "thêm file khoá đơn giản" — vi phạm điều rubric cấm (tự chế cơ chế khoá mới).
case 4 A=partial B=partial — A: không xoá hết, chạy /audit theo applicability, NT12 sửa theo bằng chứng, audit quý; thiếu doc-health last_verified/ttl_days phân loại FRESH/SUSPECT/STALE, thiếu verify-on-use bump + commit re-verify(<doc>), thiếu ý doc 6 tháng không ai chạm có thể không load-bearing. B: không xoá hết, đánh dấu "chưa xác nhận" thay vì xoá (gần verify-on-use), giảm số doc load-bearing, cơ chế chống mục định kỳ; nhưng không có /audit applicability, không có last_verified/ttl_days, không có bump kèm commit re-verify, không nêu NT12.
case 5 A=pass B=fail — A: không kích foundation, định tuyến sang ba-flow-logic, chỉ nêu operating layer sẽ đóng góp sau. B: tự làm phân tích nghiệp vụ (vai trò/luồng/ngoại lệ/AC) — vi phạm điều cấm.
case 6 A=pass B=fail — A: không kích foundation, định tuyến sang ui-ux-triage, không tự debug, chỉ nêu lớp vận hành hỗ trợ. B: tự debug defect trực tiếp (4 nguyên nhân, cách kiểm) — vi phạm điều cấm.
