case 0 A=partial B=fail — A: kích foundation, chọn profile core (không bật hết 15), init + CLAUDE.md phân tầng + app-map + hook (covers-sync/secret/encoding/budget), doctor gợi profile kế; THIẾU covers/last_verified/ttl_days trên app-map, doc-health, trục "Git giữ sự thật / ai-simple quản context-ý định-invariant-quyền ghi", trigger scale-up chỉ nói mơ hồ. B: không kích foundation, không profile, không `ai-simple init`/app-map/doc-health — tự chế husky+lint-staged; bỏ qua toàn bộ máy thực thi mà rubric đòi → sai hướng.
case 1 A=partial B=partial — A: đúng NT01/02/03, CLAUDE.md gốc <6K trỏ module, app-map, router /fl; THIẾU covers-sync trong pre-commit + doc-health TTL (chỉ nhắc hook enforce ở profile scale), không nêu rõ "vấn đề cấu trúc context, không phải đổi model". B: đúng hướng cấu trúc context (app-map, CLAUDE.md con, ADR, đọc-trước-khi-gọi) nhưng không covers, không router, không cổng máy chống mục, không doc-health.
case 2 A=pass B=partial — A: GREEN/YELLOW/RED đúng định nghĩa, RED 1 câu confirm gộp, YELLOW tự làm + Assumptions, trục authority độc lập (L1/L2/L3, reversible ≠ authorized), kết luận giảm hỏi bằng phân tầng. B: 3 tầng + 1 câu gộp + kết luận đúng, nhưng gộp gửi mail/thanh toán vào "không hoàn tác" thay vì tách trục AUTHORITY độc lập với risk.
case 3 A=partial B=fail — A: NT13, lot MECE, claim/lease qua `ai-simple parallel`, worktree per lot, merge queue, cấm stash/reset/checkout, reflog; THIẾU claim gate trong pre-commit và luật revert chỉ qua patch-file, không nhắc `restore`. B: worktree + chia phạm vi + merge tuần tự + cấm reset/checkout đúng tinh thần, nhưng không có claim/lease/registry, không `ai-simple parallel`, và đề xuất "thêm một file khoá đơn giản" = tự chế cơ chế khoá mới — điều rubric cấm.
case 4 A=partial B=partial — A: không xoá hết, /audit theo applicability, viết lại đúng phần lệch, NT12 + audit quý; nhưng dùng doc-status canonical/stale/deprecated thay vì doc-health FRESH/SUSPECT/STALE theo last_verified/ttl_days, không có verify-on-use + bump + commit re-verify, không nêu "doc không ai chạm 6 tháng có thể không load-bearing". B: không xoá hết, đánh dấu chưa xác nhận (gần verify-on-use), giảm docs load-bearing, vòng bảo trì quý; nhưng không /audit, không doc-health TTL, không cơ chế bump/re-verify commit, không NT12.
case 5 A=pass B=partial — A: không kích foundation, định tuyến rõ sang ba-flow-logic (user registry/flow/AC), không tự sinh ba-spec; phần thêm chỉ nói operating layer sau khi có spec. B: không kích foundation nhưng tự làm BA tại chỗ (vai trò/luồng/ngoại lệ/AC), không định tuyến ba-flow-logic, chưa đủ user×nghiệp vụ / cross-user handoff / Input-Output.
case 6 A=partial B=partial — A: nói không thuộc, định tuyến ui-ux-triage, không tự sửa defect; nhưng foundation vẫn "vào" với cả loạt bullet (route context, LOGIC/REQUEST, risk tier, NT04, authority) dù không xác định thiếu lớp vận hành — rubric chỉ cho foundation vào khi thiếu lớp đó. B: không kích foundation, sửa gốc + test ranh giới đúng tinh thần, nhưng debug thẳng không qua ui-ux-triage (reproduce → oracle → fix qua cổng).
