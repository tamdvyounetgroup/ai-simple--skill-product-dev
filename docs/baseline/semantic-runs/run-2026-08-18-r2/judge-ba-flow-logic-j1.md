case 0 A=partial B=pass — A: phân tích tự sự, có ma trận quyền/vòng đời nhưng thiếu registry có cron, thiếu handoff map có điều kiện+điểm kết khi treo, thiếu Input/Output start/end, không Domain-Specialist kế toán, 0 AC G/W/T (kết bằng câu hỏi thay vì spec). B: đủ registry ≥3 + cron, matrix 1 owner, handoff map có điều kiện/không dead-end, I/O start/end, Domain-Specialist kế toán, AC G/W/T không UI, không vẽ màn.
case 1 A=partial B=partial — A: bắt đúng 2 bệnh nhưng không Suggester/Optimizer, không log candidate loại, không Domain-Specialist kho, và đề xuất sửa ở tầng code/UI (khoá lạc quan, transaction, vô hiệu nút) thay vì AC+flow trong ba-spec. B: đủ handoff treo/invariant, Suggester/Optimizer, log, AC+flow, không sửa code — nhưng không có Domain-Specialist kho xác nhận giữ-tạm vs trừ-thật (chỉ nói "kho là nghiệp vụ chuyên").
case 2 A=partial B=pass — A: nhận ra là giao diện không phải BA (không kích BA) nhưng không định tuyến sang ui-design-logic mà tự làm design (bảng màu, lưới, thẻ). B: định tuyến rõ ui-design-logic, nêu ranh giới WHAT/WHY vs HOW-nhìn, không tự định nghĩa UI.
case 3 A=partial B=pass — A: đúng hướng "spec thiếu, không vá code", có AC mới cho công nợ; nhưng không Domain-Specialist soi bút toán cân, không tier/ASK, không bump last_verified/History, và bước 4 vẫn tự nhận sửa code+backfill. B: đủ reverse handoff, không sửa code, handoff H Chủ→Kế toán + AC, Domain-Specialist kế toán (bút toán đảo), tier GREEN/YELLOW, bump last_verified + History.
case 4 A=pass B=pass — A: không kích BA, coi là đổi text, chỉ đổi nhãn không đổi mã, sửa thẳng (có hỏi 1 ý về nghĩa — không phải kích BA). B: không kích BA, build thẳng, phân biệt nhãn vs enum, chỉ quay về BA nếu đổi nghĩa.
case 5 A=partial B=pass — A: tách rủi ro, không drop ngay, có xin xác nhận; nhưng tự quyết HOW-DB (định nghĩa cột bảng công nợ, expand/contract, "thêm bảng làm ngay"), không defer ai-simple #06/không nêu cổng RED, không dừng ở WHAT công nợ. B: từ chối làm thẳng, defer #06 RED, 1 ASK gộp, tách WHAT (ba-spec công nợ) khỏi HOW-DB, không tự chế tier.
case 6 A=pass B=pass — A: không kích BA, build thẳng với chi tiết lọc/sắp xếp (hợp "→ build"). B: không mở pipeline, chỉ thêm AC hành vi tối thiểu (doc-sync), pin ranh giới trình bày=design / rule=BA, đẩy HOW-nhìn sang design.
case 7 A=fail B=pass — A: không frontmatter covers/last_verified/ttl_days, không 'Load khi', không owner-user tường minh, AC không G/W/T, không `Test:`/`Assert` định lượng, không chạy ba-verify → cổng BLOCK = chưa xong. B: có registry/owner, AC G/W/T có Test e2e/integration + Assert định lượng (status=="Chờ xác nhận", count<=...), frontmatter đủ, chạy ba-verify.sh exit 0, không từ UI.
