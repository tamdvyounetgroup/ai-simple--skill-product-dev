# Giám khảo J2 — ba-flow-logic (chấm mù, vòng 2)

case 0 A=partial B=pass — A: có user/quyền/lifecycle nhưng không có ba-spec, không handoff map kèm điểm-kết-khi-treo, không Input/Output per flow, không Domain-Specialist, không AC G/W/T. B: đủ registry (+cron), matrix 1 owner, cross-user Tạo→Duyệt→Công nợ, I/O + START/END, spawn Domain-Specialist kế toán, AC G/W/T không UI, ba-spec trước design.
case 1 A=partial B=partial — A: chẩn đúng 2 bệnh nhưng trượt sang HOW-code/UI (khoá lạc quan, vô hiệu nút), không Suggester/Optimizer, không Domain-Specialist kho, không optimization log, không AC/ba-spec. B: đúng handoff treo + invariant, Suggester/Optimizer, log candidate loại, AC + flow, không sửa code — nhưng thiếu Domain-Specialist kho xác nhận giữ-tạm vs trừ-thật.
case 2 A=partial B=pass — A: nhận ra là giao diện, không kích BA, nhưng không định tuyến sang ui-design-logic/ui-ux-triage mà tự làm design. B: định tuyến rõ ui-design-logic (triage nếu defect), pin WHAT/HOW, không tự định nghĩa UI.
case 3 A=partial B=pass — A: đúng "spec thiếu, không vá code trước", thêm AC, nhưng thiếu Domain-Specialist kế toán, không tier/ASK, không bump last_verified/History, kết bằng bước sửa code. B: reverse handoff, thêm AC/flow handoff Chủ→Kế toán, Domain-Specialist soi bút toán (đảo khi huỷ), tier GREEN/YELLOW không đụng AC cũ, bump last_verified + History.
case 4 A=partial B=pass — A: không kích BA nhưng dừng lại hỏi nghĩa từ trước khi làm, không build thẳng. B: build thẳng, không BA/không spawn, chỉ nhắc giữ tên enum + sync doc/test.
case 5 A=partial B=pass — A: tách additive/destructive và hỏi trước khi drop, nhưng tự định nghĩa HOW-DB (cột bảng, expand-contract) và tự nhận làm, không defer #06/không đẩy sang cổng, không phần WHAT công nợ. B: defer ai-simple #06 RED, 1 ASK gộp, WHAT (ba-spec công nợ) tách khỏi HOW-DB, không tự chế tier, không đụng DB.
case 6 A=partial B=partial — A: build thẳng, không kích BA, nhưng không pin ranh giới trình bày=design / rule=BA, không nêu ngoại lệ "lọc kéo theo quyền → BA". B: pin ranh giới rõ + nêu điều kiện nâng tier, nhưng vẫn coi lọc/sắp là "hành vi" và viết AC vào ba-spec (BA-lite) — mâu thuẫn nhẹ với rubric "trình bày dữ liệu = design/build".
case 7 A=fail B=partial — A: không frontmatter, AC không G/W/T, không Test:/Assert định lượng, không ba-verify → cổng BLOCK, coi như chưa xong. B: frontmatter covers/last_verified/ttl_days, owner-user, AC G/W/T có Test (e2e cho đặt) + Assert định lượng, không UI, ba-verify exit 0 — nhưng thiếu 'Load khi' trong frontmatter.
