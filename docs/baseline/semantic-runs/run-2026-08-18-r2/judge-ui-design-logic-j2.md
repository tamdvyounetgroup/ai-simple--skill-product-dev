case 0 A=fail B=pass — A: không có DESIGN-SPEC trước code, dùng nguyên cam neon làm accent (rubric cấm), không stone ramp, không screenshot QA; B: spec trước, screen map có goal/step, mobile-first, accent hạ S/L đo contrast, stone, bottom tab ≤5, table→list card, screenshot 375/768/1440+dark
case 1 A=fail B=partial — A: thêm mục thứ 6 vô tư không nhắc budget, form "modal hoặc trang riêng tuỳ pattern", không cập nhật screen map; B: nhận ra 5→6 và lý giải theo [DEF] 5–7 (có lý do, kèm phương án gom khi có bottom tab), screen map trước code, nhưng form không cam kết drawer ("page hay drawer tra 03 — form dài thì page") trong khi rubric đòi drawer cho form ≤5 field
case 2 A=partial B=pass — A: liệt kê đúng lỗi (spacing, 2 primary, casing, subtitle) nhưng không screenshot trước, không phân cấp BLOCK/MAJOR/MINOR, không nêu thứ tự hình học→màu, không before/after; B: phân loại case A theo 06 §3, chấm phân cấp MAJOR (nêu rõ chưa có BLOCK vì chưa render), thứ tự spacing→text→màu, screenshot 1440/375/dark + báo cáo before/after; điều kiện screenshot trước được nêu đúng
case 3 A=fail B=pass — A: gợi ý Inter/Be Vietnam Pro/Public Sans (không giữ system stack, không nêu đường ngoại lệ [DEF]), cho phép mô tả dưới mục "khi mang thông tin thật" (rubric cấm subtitle, chỉ tooltip); B: system stack, nêu đích danh 2 cửa ngoại lệ (user chỉ font / `## Ngoại lệ đã duyệt` lý do audience-kỹ thuật), từ chối văn thuyết minh, nêu rõ 2 quyết định với user
case 4 A=partial B=pass — A: giữ HOW/WHAT tốt và có before/after, nhưng không phân loại theo 06 §3, không nói không mở Hallmark, và tự "đặt hệ thống token" toàn cục mà không confirm RED; B: phân loại case B(+A), theo 08 không mở công cụ visual ngoài cho admin, checklist 06 §2 + triệu chứng slop, RED confirm 1 câu cho token/nav, screenshot before/after
